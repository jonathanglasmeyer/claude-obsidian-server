# API Secret Key for server authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_SECRET_KEY=your_secret_key_here

# Server mode - set to 'local' for local server, 'ssh' for SSH tunnel
NEXT_PUBLIC_SERVER_MODE=ssh